/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env*.local
next-env.d.ts

.vercel
.env*

# Generated TTS narration (scripts/generate-video.ts) — build output, not source.
/src/remotion/public/audio

# Videos rendered by /studio (app/api/generate-video/route.ts) — user-generated
# content written at runtime, not source.
/public/generated

# TS incremental build cache (regenerated by tsc/next build).
*.tsbuildinfo

# Supabase CLI's local cache (version-check file, not project config).
/supabase/.temp
